# QA Review — lru-cache — 2026-06-05

**Verdict: APPROVED** · Mode: autopilot · Reviewer: qa-reviewer (read-only; report persisted by orchestrator)

Test suite re-run by reviewer: **72 passed, exit code 0** (`tests/unit/test_cache.py`). README example verified line-by-line against the code and passing tests (the `python -c` smoke run is correctly denied by the permission model; every README example line maps 1:1 to an existing passing test).

## Ticket → spec coverage

Every ticket §3 (In scope) item and every §12 (Success criteria) bullet maps to a spec requirement (R1–R18). No §4 (Out of scope) item is implemented — verified no TTL, no async, no persistence, no serialization hooks, no CLI/HTTP, no weight-based eviction. The intake "Decisions taken" deferrals are all pinned in spec §4.1 D1–D9.

## Constitution check (all clean)

- §1 stdlib-only: imports only `collections.OrderedDict` and `typing.Any`. PASS.
- §2 non-negotiables: §2.3 (no silent data loss) — eviction is the documented bounded-cache contract, surfaced via the `evictions` counter. PASS.
- §3 code style: full-word naming, one purpose per method, why-not-what comments, `ValueError` at constructor boundary, absolute import. PASS.
- §4 test discipline: spec-derived (headers cite R1–R18), unit-only, no mocks. PASS.
- §5 security: in-process, trusts caller; only validated input is `capacity`. PASS.
- §6 perf: budgets TBD; R16 structural check enforces no O(n) scan. PASS.
- §7 a11y: N/A — no UI surface. PASS.
- §8 observability: §8a justified-no-event for an in-process primitive (spec §10); §8b/c/d disabled. PASS.
- §9 Definition of Done: met. PASS.
- `CONSTITUTION.project.md`: no §P sections — nothing to contradict. PASS.

## Coverage matrix (requirement → impl → test → doc)

| Req | Impl | Test | Doc |
| --- | --- | --- | --- |
| R1 public class, sole public name | `__all__`, `class LruCache` | `test_r1_*` | README surface table |
| R2 capacity validation | `__init__` L36–37 | `test_r2_*` | README troubleshooting |
| R3 get hit | L52–55 | `test_r3_*` | README |
| R4 get miss | L56–57 | `test_r4_*` | README |
| R5 put new | L71–74 | `test_r5_*` | README |
| R6 put overwrite | L67–70 | `test_r6_*` | README |
| R7 eviction at capacity | L71–73 | `test_r7_*` | README |
| R8 true-LRU recency | L53,69,72 | `test_r8_*` | README |
| R9 None storable | L52–55,82–84 | `test_r9_*` | README |
| R10 five stats read-only | properties L123–146 | `test_r10_*` | README |
| R11 reset_stats | L105–113 | `test_r11_*` | README |
| R12 contains/peek non-recency | L76–84,115–117 | `test_r12_*` | README |
| R13 remove (bool, no raise) | L86–95 | `test_r13_*` | README |
| R14 clear (entries only) | L97–103 | `test_r14_*` | README |
| R15 unhashable → TypeError | natural propagation | `test_r15_*` | README |
| R16 amortized O(1) | OrderedDict, no scans | `test_r16_*` | README |
| R17 not thread-safe, no lock | docstring, no lock | `test_r17_*` | README |
| R18 docstrings | all methods documented | `test_r18_*` | source docstrings |

Every requirement has impl + test + doc coverage. Spec §7 failure modes each tested and documented.

## Spec §9 open-questions intent confirmation

- OQ-1 delegated decisions: all pinned, consistent with ticket §14 and constitution. Confirmed.
- OQ-2 non-`int` capacity: `if capacity < 1` (L36). `0.5` → `ValueError`; `"3"` → natural `TypeError`. Matches §7 "not silently accepted." Noted as minor for optional hardening.
- OQ-3 both `__contains__` and `peek`: matches ticket §13 Q7. Confirmed.
- OQ-4 `remove`→`bool`, `put`→`None`: matches §5. Confirmed.
- OQ-5 stdlib/in-memory/TypeError/no-observability/no-perf-SLA: consistent with §1/§6/§8. Confirmed.

## Minor findings (→ BACKLOG.md, non-blocking)

- **[minor]** No explicit `int` type guard on `capacity` (OQ-2). `LruCache(1.5)` accepted as `1.5`; `LruCache(True)` accepted as `1`. Within spec (§7 only requires non-comparable types not be silently accepted) but a latent surprise. `src/tokenlab/cache.py:36`. Filed as B-007.
- **[minor, informational]** R16 large-capacity test is a correctness smoke check, not a timing budget (accurate per §6). No action required.

## Verdict

**APPROVED.** Zero blocker, zero major REJECTs. Spec ↔ implementation ↔ tests ↔ docs are consistent; §9 Definition of Done met. One actionable minor finding recorded for backlog (B-007); neither minor blocks convergence.
