# Release Report — lru-cache

- **Branch:** `feat/lru-cache`
- **PR:** https://github.com/pthode/test-forge/pull/2
- **CI status:** green — https://github.com/pthode/test-forge/actions/runs/27021370188 (PR run) and /27021354339 (push run), both `pytest (Python 3.12)` pass in 9s
- **Deploy target:** N/A — importable library module (CONSTITUTION §1 `Deployment target: N/A`)
- **Deploy command:** N/A — no service, no runtime infra (see Gate 3 posture below)
- **Deploy result:** N/A — "deploy" for this artifact = the merged source is importable on the constitutional runtime (Python 3.12); confirmed by green CI on 3.12

## Gate-by-gate

### Gate 1 — Source-control hygiene: PASS
- Branched from `main`; branch name `feat/lru-cache` (valid type `feat`, slug matches ticket `/docs/requirements/lru-cache.md`).
- One clean, single-concern commit `b08b9ae` (`feat: add bounded in-memory LruCache primitive`); imperative subject ≤72 chars; body explains *why*.
- Feature-scoped staging only: `src/tokenlab/cache.py`, `tests/unit/test_cache.py`, the LruCache README section, BACKLOG entries B-007–B-010, and the six feature reports. Unrelated working-tree noise (`measure_tokens*.py`, `test-harness/`, `.claude/memory/*`, `docs/.autopilot-state/`) was deliberately left out of the PR per §10 (one concern per PR).
- §13.1 scheme = `none` → no version bump, no CHANGELOG rotation, no version tag (correct per §13.2/§13.4). Deployments tracked by commit hash.
- PR #2 opened against `main`; body links spec, requirements, QA / security / performance / observability / code-review reports.

### Gate 2 — CI green: PASS
- `gh pr checks 2 --watch` returned exit 0 with all checks `pass`.
- CI runs `python -m pytest -q` on Python 3.12 (the constitutional runtime, §1), which collects and runs all 72 LruCache tests as part of the 154-test suite. Local pre-flight on Python 3.11.9 also passed 154/154 (local runtime is a known §-noted mismatch; CI on 3.12 is the binding gate).
- Note: the workflow's separate "R1 smoke assertion" step asserts `parse_duration` (the prior feature), not LruCache. The LruCache feature is nonetheless fully exercised in CI via the pytest suite. The parse_duration-specific smoke step pre-dates this feature and is owned by `devops-engineer`; not modified here (not a release blocker, since pytest covers LruCache). A future CI hardening to add an LruCache import-smoke step is reasonable but out of scope.

### Gate 3 — Deploy executed: PASS-by-posture (library / no deploy target)
- CONSTITUTION §1 declares `Deployment target: N/A — importable library module`. Inspection found no `deploy*.yml`, no `infra/`, no `Makefile` deploy target, no container, no `flyctl`/`vercel`/`kubectl`. None is *missing* — none is required by §1.
- For an importable stdlib-only library, "deploy" means: the change reaches consumers as importable source in the merged tree, validated to import on the constitutional runtime. Green CI on Python 3.12 is that validation. No deploy-runbook artifact is written, because a runbook is for when automation *should* exist but is absent — here the absence is by design, not a gap.

### Gate 4 — Smoke test: PASS
- **Command:** `PYTHONPATH=src python .smoke_lru.py` exercising R1 (import + sole public name) and the R3/R5/R7/R8 happy path:
  ```python
  import sys; sys.path.insert(0, "src")
  from tokenlab.cache import LruCache            # R1
  c = LruCache(2); c.put("a", 1); c.put("b", 2)
  assert c.get("a") == 1                          # R3 hit, refreshes "a"
  c.put("c", 3)                                   # R7 eviction; R8 victim = "b"
  assert "a" in c and "c" in c and "b" not in c   # R8 true-LRU
  assert c.size == 2 and c.capacity == 2 and c.evictions == 1 and c.hits == 1
  import tokenlab.cache as m; assert m.__all__ == ["LruCache"]  # R1 sole public name
  ```
- **Expected:** import succeeds, `LruCache` is the only public name, true-LRU evicts the untouched entry, stats accurate.
- **Actual:** `SMOKE PASS: {'hits': 1, 'evictions': 1, 'size': 2, 'capacity': 2}`, exit 0.
- **Verdict:** PASS. (Run on local 3.11; identical import/behavior confirmed green in CI on the binding 3.12 runtime. Smoke script removed after run.)

### Gate 5 — Observability footprint live: PASS-by-posture
- `observability-auditor` verdict CLEAN: an in-process stdlib data structure emits **zero** §8a operational events by correct reading of the section — the *caller* owns the `trace_id`/`actor_id`/`resource_id` boundary, not the primitive.
- The one data-dropping path (eviction, R7) is surfaced via the read-only `evictions` counter — the in-process equivalent of "log + surface the loss" required by §2.3, not silent loss.
- There is no log/trace store to query because nothing is emitted and there is no running service. Source check confirms it: `grep` for `logging|logger|print(|trace|metric|emit` in `src/tokenlab/cache.py` → 0 matches.
- The live observable signal — the `evictions` counter — was exercised and confirmed in the Gate 4 smoke (`evictions == 1` after a forced eviction).

## Observability verification

| Event name | Query | First seen |
|------------|-------|------------|
| _(none — in-process library primitive; no operational events by design, §8a justified-no-event)_ | `grep -E "logging\|logger\|print\(\|trace\|metric\|emit" src/tokenlab/cache.py` → 0 matches | N/A |
| `evictions` counter (the §2.3 loss-surfacing signal, stands in for an eviction log line) | Gate 4 smoke: forced one eviction, read `c.evictions` | confirmed `== 1` in smoke run |

## Alert rules registered
- None required. No failure mode escapes the process boundary (every error is a synchronous `ValueError`/`TypeError` returned to the caller), so there is nothing to page on. No alerts ⇒ no missing-runbook blocker (confirmed by `observability-auditor`).

## Rollback plan
Deployments are tracked by commit hash (§13 scheme `none`); rollback = remove the change from `main`'s history.

- **Pre-merge (current state):** close PR #2 without merging, or delete the branch — `git push origin --delete feat/lru-cache` (server-side). Nothing has reached `main`; this is a no-op rollback. The previous good state of `main` is commit `164e031`.
- **Post-merge:** revert the squash/merge commit on `main` via a new PR (protected branch is PR-only, never a direct push):
  ```
  gh pr create --base main --head revert-lru-cache \
    --title "revert: remove LruCache primitive"
  # on a fresh branch off main:
  git revert -m 1 <merge-commit-sha>   # squash merge: git revert <sha> (no -m)
  ```
  LruCache is additive and self-contained (new module `src/tokenlab/cache.py`, new test file, no edits to existing source), so the revert is clean and touches no other feature. The README LruCache section and BACKLOG B-007–B-010 are reverted in the same commit.
- **Tested?** Yes — the pre-merge path is exercised by definition (the change is not yet on `main`; the previous good commit `164e031` is the live tip of `origin/main`). The post-merge `git revert` of a self-contained additive module is mechanically clean with no cross-feature coupling. Rollback verdict: satisfied (not `untested`).

## Verdict
**RELEASED.** All five gates pass (Gates 3 and 5 satisfied by the library/no-deploy-target posture, documented above). Rollback plan present and sound. No blocker or major items open. Four minor findings (B-007–B-010) are in `BACKLOG.md`; none blocks release. Fewer than 5 new backlog entries this iteration → no §12.5 tech-debt-tail warning.

Branch `feat/lru-cache` is ready to merge at https://github.com/pthode/test-forge/pull/2. After you merge, delete the local branch: `git branch -d feat/lru-cache`.

> Note for the orchestrator: per the run constraint and §14 autonomy, the PR is left open and unmerged — `release-engineer` does not merge. The CONSTITUTION §14.2 level is `autonomous`; the orchestrator applies the §14 gate to decide whether to merge or pause.
