# Code Review — lru-cache — 2026-06-08 (1.17.2 run)

**Reviewer: code-reviewer** (read-only; persisted by orchestrator). Artifact: `src/tokenlab/cache.py`. Binding: CONSTITUTION §3.

## Iteration 1 → 2 outcome

- **Iteration 1:** emitted a `major` REJECT to spec-architect over `LruCache` vs `LRUCache` (§3 ≤3-letter acronym casing).
- **Resolution:** spec-architect kept the user-locked name `LruCache`, documented the deliberate §3 deviation as spec §5.1 **D9** + §9, and downgraded the finding to minor (tracked as B-008). Both literal REJECT options were correctly refused — a §P exception would violate §11.2; a rename would overwrite an explicit user requirement.
- **Iteration 2:** **No REJECT.** Finding resolved as tracked minor B-008. §3 is a style anchor, not a §2 non-negotiable; a user's locked requirement (ticket §1/§3) outranks it. No new argument warrants re-raising to major.

## Quality summary (iteration 1)

- must-fix: 0 · should-fix: 2 (naming [resolved via D9]; `put` cohesion [optional `_evict_lru` extraction]) · nit: 4
- Implementation is structurally clean: single-purpose functions, no duplication, no leaky abstraction; `OrderedDict`-based LRU consistent with D1/D2.

## Minor findings → BACKLOG (filed)
- B-012 — `capacity` is a mutable public attribute (cache.py:20)
- B-013 — keys/values typed `Any`, no TypeVar generics (cache.py:21)
- B-014 — module docstring not verb-first imperative (cache.py:1)
- B-015 — add why-comment on `move_to_end` in put update path (cache.py:35-37)

The `put` cohesion observation (extract `_evict_lru`) was raised as should-fix but not formalized as a REJECT; behavior is correct. Recorded here for the record; can be folded into a future refactor pass.

**No REJECT outstanding. Convergence may close.**
