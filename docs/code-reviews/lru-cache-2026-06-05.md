# Code Review — lru-cache — 2026-06-05

**Reviewer: code-reviewer** (read-only; report persisted by orchestrator)
Artifact: `src/tokenlab/cache.py` · Reference style: `src/tokenlab/duration.py` · Binding: CONSTITUTION §3 (no §P sections)

## Summary

- must-fix: 0
- should-fix: 0
- nit: 2

No `blocker` or `major` quality issues. **No REJECT to developer.** The implementation is cohesive (one purpose per method), free of meaningful duplication, introduces no premature abstraction, and matches the house style established by `duration.py` (module docstring, stdlib-only imports, `f"...{value!r}"` error messages, why-not-what comments, absolute imports).

## Findings

**1. [nit] Naming — `LruCache` vs §3 acronym casing.** §3 says acronyms ≤3 letters stay uppercase (`URL`, `ID`); strict reading wants `LRUCache`. **However the name `LruCache` is mandated by the spec** (R1, §4, §5 class surface), so the developer correctly implemented to spec — not a developer defect. The divergence originates upstream; route a reconciliation note to `spec-architect`, not a REJECT to developer. `cache.py:9`. Filed as B-008.

**2. [nit] `get` hit path does two hash lookups.** `cache.py:52-55` does `if key in self._store: ... return self._store[key]` — `__contains__` then `__getitem__`. A sentinel-based single lookup (`self._store.get(key, _MISSING)`) would avoid the double probe. Purely stylistic; current form is more readable and the double lookup is amortized O(1), so R16 is unaffected. `cache.py:52-55`. Filed as B-009.

## Out of scope

- Class-name §3 reconciliation forwarded to `spec-architect` (see nit 1).
- No correctness concerns observed; correctness is qa-reviewer's call.

No REJECT block emitted. Both nits go to BACKLOG.md per §12.1.
